import komoverseAxiosIns from 'helper/headers';

// const request = {
//   get: async (url: string) => {
//     const promiseResult = await komoverseAxiosIns.get(url);
//     return promiseResult ? promiseResult.data : [];
//   },
//   post: async ({ url, body = {} }: any) => {
//     const promiseResult = await komoverseAxiosIns.post(url, body);
//     return promiseResult ? promiseResult.data : {};
//   },
//   put: async (url: string, data: any) => {
//     const promiseResult = await komoverseAxiosIns.put(url, data);
//     return promiseResult ? promiseResult.data : [];
//   },
//   delete: async (url: string) => {
//     const promiseResult = await komoverseAxiosIns.delete(url);
//     return promiseResult ? promiseResult.data : [];
//   },
// };

// export default request;
